[pytest]
# Living here (not in the project root's pyproject.toml) so pytest's
# rootdir resolution stops at tests/ and never tries to import the
# package's __init__.py, which requires ComfyUI's `server` module at
# import time.
addopts = --import-mode=importlib
